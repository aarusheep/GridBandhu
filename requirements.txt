# Data handling
pandas>=2.0.0

# Postgres (static topology data)
psycopg2-binary>=2.9.0

# Redis (live telemetry data)
redis>=5.0.0

# Scheduling (detection loop + telemetry generator)
apscheduler>=3.10.0

# Environment variables (.env file)
python-dotenv>=1.0.0

# HTTP telemetry ingestion and validation
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0